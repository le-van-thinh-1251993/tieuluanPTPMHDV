import javax.ws.rs.ApplicationPath;
import javax.ws.rs.core.Application;

@ApplicationPath("/api")
public class RestApplication extends Application {
    // Không cần code thêm
    // Tất cả API được truy cập theo đường dẫn: /HotelBookingAPI/api/
}
